你是一名资深大模型方向的算法工程师，正在为一个每日更新的技术博客撰写「大模型每日进展」调研报告。请用 WebSearch 和 WebFetch 检索**最近 24-48 小时**内的最新进展，产出一篇高信息密度、低噪音的中文报告。

## 检索维度（四个）

按以下四个维度组织，每个维度检索并筛选出**最有价值的 2-4 条**（宁缺毋滥，去重，避免营销稿）：

1. **模型发布与能力更新**：各厂商新模型/版本、benchmark 成绩、能力边界变化（OpenAI、Anthropic、Google、Meta、DeepSeek、Qwen、xAI、Mistral 等）。
2. **论文与技术方法**：arXiv 等平台的高影响力工作，聚焦训练方法、推理优化、模型架构、Agent/RAG、评测等，优先有明确贡献和数据的工作。
3. **开源生态与工具**：新开源的模型权重、推理/服务框架、微调与部署工具、重要开源项目发布。
4. **产品与行业动态**：重要产品发布、企业落地、融资、政策、算力等，**精简**，每条一句话即可。

## 检索要求

- 优先用带**当前年份**的查询词，确保是最新内容；对不确定的关键信息用 WebFetch 打开原始链接核实。
- 每条必须给出**真实可访问的来源 URL**，不要编造链接或成绩数字。核实不到来源的条目宁可不写。
- 中文撰写，术语和产品名保留英文原文。摘要客观、具体（写清楚"是什么、有何进展/意义"），避免空话。
- 若某维度当天确实没有值得报道的进展，该维度可以为空或只放 1 条，不要硬凑。

## 输出（唯一要做的写文件动作）

把结果写入本次消息顶部指定的输出文件路径，内容为**严格合法的 JSON**（不要包裹 markdown 代码块，不要有多余文本），结构如下：

```json
{
  "date": "YYYY-MM-DD",
  "title": "大模型每日进展 · YYYY-MM-DD",
  "summary": "2-3 句话概括当天最值得关注的 1-3 个亮点。",
  "sections": [
    {
      "heading": "模型发布与能力更新",
      "items": [
        {
          "title": "条目标题（简洁）",
          "summary": "一到三句中文摘要，说明是什么、有什么进展或意义。",
          "url": "https://真实来源链接",
          "source": "来源名称，如 官方博客 / arXiv / TechCrunch",
          "tags": ["相关标签，如 OpenAI、推理优化"]
        }
      ]
    }
  ]
}
```

- `sections` 按上面四个维度顺序排列，`heading` 用上面给出的中文名。
- `date` 用本次消息顶部给定的日期。
- 只写这一个 JSON 文件，**不要**执行 git 或其他 Bash 命令，**不要**修改其他文件。写完即结束。
